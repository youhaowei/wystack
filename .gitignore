node_modules
dist
dist-electron
.turbo
*.tsbuildinfo
.env
.env.local
data/pglite
.*-test
plugins/
.next
storybook-static
build/
.claude/
.wystack/
.wystack.json

# Clawpatch local state (first-run seed; map + findings live in the shared state dir)
.clawpatch/
