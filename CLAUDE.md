# WyStack — Claude Code

Project instructions live in **[AGENTS.md](./AGENTS.md)** — architecture, stack,
dialect policy, and commands. Read it first; all of it applies to Claude.

This file is for Claude-specific guidance only (Claude Code workflow notes, tool
preferences, slash-command conventions). Nothing yet — add it here as it comes up,
and keep generic, agent-agnostic instructions in `AGENTS.md`.

Internal links (Notion design docs, dashboards) never go in a committed file —
this repo is public. Keep them in `CLAUDE.local.md`, which is gitignored.
